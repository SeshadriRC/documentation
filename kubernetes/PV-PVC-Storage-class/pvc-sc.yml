apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: simplybyte-pvc
spec:
  storageClassName: simplybyte-storage-class
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
