- VM takes space separately for OS, RAM and it will take lot of time to boot.
- Docker will take only less space and its startup time is very fast. It won't use separate OS, it will use the base OS.
- We will create the dockerfile, docker daemon used to build the docker image and run the docker container.

<img width="888" height="513" alt="image" src="https://github.com/user-attachments/assets/fb2e50c0-58a8-40f5-8a20-a62001779b08" />


