```bash
# install specific python release
https://www.python.org/ftp/python/3.12.8/
py -3.12 -m venv .venv
source .venv/bin/activiate # linux
source .venv/Scripts/activate   # windows
deactivate # only if you want to exit
py --version
py -3.12 -m pip install dvc
```
